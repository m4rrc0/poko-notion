---
import Debug from 'astro/debug';
import { getSettings, getAllPages } from '../services/notion.js'


export async function getStaticPaths() {

	const settings = await getSettings()
	// const settings = await getAllPages()

  // const organizationsStaticPaths = organizations.map(organization => {
  //   return { params: { path: organization.path }, props: { lang, organization } }
  // })

  const staticPaths = [
    { params: { path: 'blog' }, props: { settings } },
    // ...organizationsStaticPaths,
  ]

  return staticPaths
}


const { path } = Astro.params;
const {
  settings,
  lang,
  organization, organizations,
  event, events,
  article, articles,
  area, areas,
  } = Astro.props;
---

<Debug {settings} />